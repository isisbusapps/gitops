apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: message-broker-admin
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - list:
      elements:
        # Names of clusters to deploy the app to
        - name: dev-v3
        # Uncomment if you want to deploy to dev-microk8s-alternative
        #- name: dev-microk8s-alternative
  template:
    metadata:
      name: '{{.name}}-message-broker-admin'
      annotations:
        argocd.argoproj.io/sync-wave: "0"        
    spec:
      project: infrastructure-project
      source:
        path: components/shared/message-broker-admin/message-broker-admin-chart
        repoURL: 'https://github.com/isisbusapps/gitops'
        targetRevision: main
        helm:
          valueFiles:
            - ../../message-broker/rmq-message-broker-chart/values.yaml
            - values.yaml
      destination:
        namespace: apps
        name: '{{.name}}'
      syncPolicy:
        automated: 
          selfHeal: true