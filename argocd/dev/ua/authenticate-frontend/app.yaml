apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: authenticate-frontend
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - list:
      elements:
        # Names of clusters to deploy the app to
        - name: dev-v3
        # Uncomment if you want to deploy to dev-microk8s-alternative
        #- name: dev-microk8s-alternative
  template:
    metadata:
      name: '{{.name}}-authenticate-frontend'
      annotations:
        argocd.argoproj.io/sync-wave: "0"
        notifications.argoproj.io/subscribe.on-sync-failed.smtp-ua: BISAPPSSINK@stfc.ac.uk
        notifications.argoproj.io/subscribe.on-health-degraded.smtp-ua: BISAPPSSINK@stfc.ac.uk
        notifications.argoproj.io/subscribe.on-sync-status-unknown.smtp-ua: BISAPPSSINK@stfc.ac.uk

    spec:
      project: u-a-project
      source:
        path: components/ua/authenticate-frontend/overlays/dev
        repoURL: 'https://github.com/isisbusapps/gitops'
        targetRevision: main
      destination:
        namespace: apps
        name: '{{.name}}'
      syncPolicy:
        automated: 
          selfHeal: true
