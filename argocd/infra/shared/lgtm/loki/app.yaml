apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki
  namespace: argocd
spec:
  project: infrastructure-project
  sources:
    - repoURL: 'https://grafana-community.github.io/helm-charts'
      chart: loki
      targetRevision: 17.1.* #Chart version
      helm:
        releaseName: loki
        valueFiles:
        - $values/lgtm/loki/helm-values/values.yaml
    - repoURL: https://github.com/isisbusapps/observability
      targetRevision: develop
      ref: values
  destination:
    namespace: lgtm
    name: infra
  syncPolicy:
    automated: 
      selfHeal: true