apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: fap-booklet-generator
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchLabels:
          appEnvironment: prod
  template:
    metadata:
      name: '{{.name}}-fap-booklet-generator'
    spec:
      project: r-a-project
      source:
        path: components/ra/fap-booklet-generator/overlays/prod
        repoURL: 'https://github.com/isisbusapps/fap-booklet-generator'
        targetRevision: master
      destination:
        namespace: apps
        name: '{{.name}}'
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jqPathExpressions:
            - .spec.template.spec.containers[0].image
      syncPolicy:
        automated: {}