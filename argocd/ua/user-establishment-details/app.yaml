apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: user-establishment-details
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchLabels:
          appEnvironment: dev
  ignoreApplicationDifferences:
    - jsonPointers:
        - /spec/syncPolicy
  template:
    metadata:
      name: '{{.name}}-user-establishment-details'
      annotations:
        argocd-image-updater.argoproj.io/image-list: devimage=harbor.stfc.ac.uk/isisbusapps/user-establishment-details:dev
        argocd-image-updater.argoproj.io/devimage.update-strategy: digest
        argocd-image-updater.argoproj.io/write-back-method: git
        argocd.argoproj.io/sync-wave: "0"
    spec:
      project: u-a-project
      source:
        path:  components/ua/user-establishment-details/overlays/dev
        repoURL: 'https://github.com/isisbusapps/gitops'
        targetRevision: main
      destination:
        namespace: apps
        name: '{{.name}}'
      syncPolicy:
        automated: 
          selfHeal: true
