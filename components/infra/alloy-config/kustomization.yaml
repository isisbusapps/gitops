apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - files:
      - config.alloy
    name: alloy-config
    namespace: alloy
resources:
  - static-secret.yaml