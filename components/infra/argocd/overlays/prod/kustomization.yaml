apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
- ../../base
- argocd-ingress.yaml

configMapGenerator:
  - name: argocd-cm
    behavior: merge
    literals:
      - url=https://argocd.developers.facilities.rl.ac.uk
