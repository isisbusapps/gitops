apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: user-claims-backend

namespace: apps

resources:
  - deployment.yml
  - service.yml
  - httproute.yml
  - static-secret.yml
