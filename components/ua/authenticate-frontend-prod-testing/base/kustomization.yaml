apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
- includeSelectors: true
  includeTemplates: true
  pairs:
    app: authenticate-frontend-pre-prod-testing

namespace: apps

resources:
  - deployment.yaml
  - ingress.yaml
  - service.yaml
