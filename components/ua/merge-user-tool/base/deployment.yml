apiVersion: apps/v1
kind: Deployment
metadata:
  name: merge-user-tool
spec:
  replicas: 1
  template:
    metadata:
      annotations:
        co.elastic.logs/processors.add_fields.target: ""
        co.elastic.logs/processors.add_fields.fields.application.name: "merge-user-tool"
        co.elastic.logs/processors.add_fields.fields.application.domain: "merge-user-tool"
        co.elastic.logs/processors.add_fields.fields.application.tier: "service"
        co.elastic.logs/processors.add_fields.fields.application.language: "java"
        # Extract log_level, class, and log_message as separate fields for elastic
        co.elastic.logs/processors.dissect.tokenizer: "%{?date} %{?time} %{log_level->} [%{?Session_and_user_data}] [%{class}] %{log_message}"
        co.elastic.logs/processors.dissect.target_prefix: ""
        co.elastic.logs/multiline.pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}"
        co.elastic.logs/multiline.negate: "true"
        co.elastic.logs/multiline.match: "after"
    spec:
      containers:
        - name: merge-user-tool
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: merge-user-tool-config
              mountPath: /FBS/Apps/spring/merge-user-tool
              readOnly: true
      restartPolicy: Always
      imagePullSecrets:
        - name: registry-creds
