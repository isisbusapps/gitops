apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-exchange-publisher
spec:
  revisionHistoryLimit: 3 
  selector:
    matchLabels:
      app: user-exchange-publisher      
  replicas: 1
  template:
    metadata:
      labels:
        app: user-exchange-publisher       
    spec:
      initContainers:
        - name: setup-h2-permissions
          image: ghcr.io/isisbusapps/user-exchange-publisher@sha256:33558dde0c5d13b3142838b94153f70d4e99e3a5014a67f1012d1c2d47b0d14d
          command: ["/bin/sh", "/scripts/setup_h2_permissions.sh"]
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          volumeMounts:
            - name: data
              mountPath: /data
            - name: script
              mountPath: /scripts
              readOnly: true
      containers:
        - name: user-exchange-publisher
          image: ghcr.io/isisbusapps/user-exchange-publisher@sha256:33558dde0c5d13b3142838b94153f70d4e99e3a5014a67f1012d1c2d47b0d14d
          ports:
            - containerPort: 8080            
          volumeMounts:
            - name: data
              mountPath: "/data"                       
          envFrom:
            - secretRef:
                name:  user-exchange-publisher
          securityContext:
            runAsUser: 773
            runAsGroup: 773
                      
      imagePullSecrets:
        - name: registry-creds
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: user-exchange-publisher-data-pvc
        - name: script
          configMap:
            name: setup-h2-permissions
            defaultMode: 0755    # makes script executable    
